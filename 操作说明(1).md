Work Flow:
1.开一个单独的bash,启动服务器
2.开一个

cd /home/franka/tactile/hil-serl-pi

的bash,用来检查hdf5数据
3.开一个

cd /home/franka/tactile/hil-serl-pi

的bash,用来回放pkl动作
3.开一个

cd ~/WorldRLCodes/OpenPI-series-deployments-on-Franka的bash

,用来运行推理

4.开一个claude用来处理小脚本适配性的问题
5.启动cursor解决推理脚本等较大项目的问题


1.启动服务器
conda activate tactile
cd /home/franka/tactile/hil-serl-pi/serl_robot_infra/robot_servers
bash launch_left_server.sh
每次重启需要先lsof -i :5000
然后kill -9对应的端口
然后pkill -f python防止没有杀死干净程序

2.数据回放

2.0视频回放
conda activate tactile
cd /home/franka/tactile/hil-serl-pi/
python play_hdf5_video.py /home/franka/tactile/data/WorldRL-pickplace-bread-full/position3_20260331/9.hdf5

在bash脚本 "/home/franka/tactile/hil-serl-pi/play_hdf5.sh" 中修改播放路径

gt hdf5:/home/franka/tactile/data/WorldRL-pickplace-bread-full/position3_20260331/9.hdf5

2.1动作回放

conda activate tactile
cd /home/franka/tactile/hil-serl-pi
python trajectory_replay.py --input /home/franka/tactile/data/WorldRL-pickplace-bread-full/position3_pkl/9.pkl --server-url http://127.0.0.1:5000 --home --gripper-lead-frames 60
ATTENTION:本脚本基于 硬编码延迟修正信息 修复了了夹爪输出延迟问题


python play_video.py /home/franka/tactile/data/WorldRL-pickplace-bread-full/position3_20260331/9_tactile.mp4
*************************************************************************
/////////////////////////////////////////////////////////////////////////

WARNING:如果你想停下机械臂运动，立即切到franka的浏览器界面并按下右侧Mode 中的 Porg按钮。franka有基础的自动急停,但碰撞仍然可能造成不可逆的损害.

/////////////////////////////////////////////////////////////////////////
*************************************************************************


gt pkl:/home/franka/tactile/data/WorldRL-pickplace-bread-full/position3_pkl/9.pkl
idm pkl:
inference pkl:
注意：如果你想停下机械臂运动，切到franka的浏览器界面并按下右侧Mode 中的 Porg按钮。
2.2检查格式
hdf5:

conda activate tactile
cd /home/franka/tactile/hil-serl-pi
python read_hdf5.py 

pkl:

conda activate tactile
cd /home/franka/tactile/hil-serl-pi
python read_pkl.py /home/franka/WorldRLCodes/OpenPI-series-deployments-on-Franka/testdata/eef1.pkl

2.3 食品保存
cd /home/franka/tactile/hil-serl-pi
python save_hdf5_video.py data/xxx/0.hdf5 -c front_image -f 10 -o my_video.mp4


3.蔬菜


3.1 基本蔬菜
conda activate tactile
cd /home/franka/tactile/hil-serl/examples
python record_demos_hdf5_realsense.py --xense_serial_left=OG000191 --xense_serial_right=OG000248
这里注意修改默认保存路径，在record_demos_hdf5_realsense.py的args里面
r是归位
s是开始
注意夹爪的操作:o是开启夹爪,c是关闭夹爪
w是写入
q是结束遥操，切记不要ctrl+c（否则数据白采）

保存格式是hdf5

3.2 带六维力的蔬菜

//////////////////////////////////
ATTENTION:tactile的预处理
//////////////////////////////////

conda activate tactile
cd /home/franka/tactile/hil-serl/examples
python /home/franka/tactile/hil-serl-pi/examples/record_demos_hdf5_realsense_force.py --xense_serial_left=OG000191 --xense_serial_right=OG000248
其余同上

4.推理pi05
1. 启动机器人 Server 端
conda activate tactile
cd ~/tactile/hil-serl-pi/serl_robot_infra/robot_servers
bash launch_left_server.sh
2. 启动推理（默认参数）
conda activate tactile
cd ~/WorldRLCodes/OpenPI-series-deployments-on-Franka
uv run python inference_pi_franka.py
3. 自定义参数启动
conda activate tactile
        cd ~/WorldRLCodes/OpenPI-series-deployments-on-Franka
        uv run python inference_pi_franka.py \
            --checkpoint_dir ./model/30000 \
            --config_name pi05_franka_low_mem_finetune \
            --action_mode ee_delta \
            --hz 20 --debug_log auto
4.纯测试输出格式
cd ~/WorldRLCodes/OpenPI-series-deployments-on-Franka
uv run python inference_pi_franka.py \
    --checkpoint_dir ./model/30000 \
    --config_name pi05_franka_low_mem_finetune \
    --action_mode ee_delta \
    --hz 20 --debug_log auto --show_action_info \
    --dry_run 

IDM数据回放:

conda activate tactile
cd /home/franka/tactile/hil-serl-pi
python trajectory_replay.py --input /home/franka/WorldRLCodes/OpenPI-series-deployments-on-Franka/testdata/eef1.pkl --server-url http://127.0.0.1:5000 --home

5.数据转换
5.0位置数据融合处理：
cd /media/liushengbang/RoboTwin2.0/policy/pi05/WorldRLdata/WorldRL-pickplace-bread-full
python merge_hdf5.py \
  --src_dirs position0_20260331 position1_20260331 \
  --output_dir merged_pos01

cd /media/yanyuyang/WRL/openpi
uv run examples/libero/convert_own_data_to_lerobot.py \
  --data-dir /media/liushengbang/RoboTwin2.0/policy/pi05/WorldRLdata/WorldRL-pickplace-bread-full/position0_20260331 \
  --repo-name pickplace_bread_pos0 \
  --output-dir /media/liushengbang/RoboTwin2.0/policy/pi05/WorldRLdata/processed_position0_20260331 \
  --action-interval 1 \
  --overwrite \
  --task "pick up the bread and put it in the plate"

后续详细操作参见：/media/yanyuyang/WRL/openpi/docs/训练流程操作文档.md
输出的格式应该是7dim eef + 1dim gripper.
todo:修好gripper的输出 OK
1.force蔬菜脚本延迟,键盘操控失灵
2.尝试pi05现有model部署,检验action space.如果验证数据大失败,面包数据重新处理.
3.idm reeplay 检验生成效果
4.跟ljm索要传感器